import { Ajv, Options } from "@fastify/ajv-compiler/node_modules/ajv";

export type ValidatorCompiler = (
  externalSchemas: unknown,
  options: Options,
  cache: Options["cache"]
) => Ajv;

export declare function ValidatorSelector(): ValidatorCompiler;

export type { Ajv, Options } from "@fastify/ajv-compiler/node_modules/ajv";

export default ValidatorSelector;
